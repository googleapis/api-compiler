syntax = "proto2";
package protiary.test;

message In {
}

message Out {
}

service TestService {
  // 1
  rpc Call1(In) returns (Out);
}

service TestService2 {
  // 1
  rpc Call1(In) returns (Out);
}
